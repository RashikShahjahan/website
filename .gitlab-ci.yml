image: publysher/hugo

before_script:
  - hugo version

pages:
  stage: deploy
  environment: Production
  script:
    - hugo
    - cp -R ./builds/* /var/www/website
  artifacts:
    paths:
    - public
  only:
  - master
