image: publysher/hugo

before_script:
  - apt-get update -qq && apt-get install -y -qq rsync

pages:
  stage: deploy
  environment: Production
  script:
  - hugo
  artifacts:
    paths:
    - public
  only:
  - master
